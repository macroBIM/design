# squaretube.csv — 각형강관 규격표

`hsection.csv` 와 같은 자리에 놓이는 각형강관(정사각) 목록. 152행.

| 규격 | 행 | 출처 |
|---|---|---|
| `SPSR` | 57 | 일반구조용 각형강관, KS D 3568 (JIS G 3466) |
| `ROLL` | 46 | 건축구조용 냉간 롤 성형 각형강관, 200×200 ~ 550×550 |
| `PRESS` | 49 | 건축구조용 냉간 프레스 성형 각형강관, 600×600 ~ 800×800 |

## 열

| 열 | 내용 |
|---|---|
| 호칭치수 | `100x100x6` — A×B×t |
| 단위무게 | kg/m. **형상에서 계산한 값** |
| A, B, t | mm |
| r | 바깥 모서리 반지름 mm |
| 단면적 | cm². **형상에서 계산한 값** |
| 규격 | SPSR / ROLL / PRESS |
| 주문규격 | `O` 면 주문 생산 (SPSR 80×80, 90×90) |
| 표기중량 | 원표에 인쇄된 kg/m. **대조용으로만 남겨둠** |

## 어떻게 만들었나

원표에서 **A, B, t 세 열만** 옮겨 적었다. 나머지는 전부 계산이다.

```
바깥 = A·B − (4−π)·r²
안쪽 = (A−2t)(B−2t) − (4−π)·(r−t)²        r−t < 0 이면 0
단면적 = 바깥 − 안쪽
단위무게 = 단면적 × 7.85 g/cm³
```

계산한 무게를 원표의 kg/m 과 매 행 대조했다. **평균 오차 0.12%.**
숫자를 잘못 옮겨 적으면 무게가 어긋나서 드러나므로, 옮겨 적기가 스스로를 검사한다.

## r 은 어디서 왔나

세 표 중 둘만 모서리 반지름을 밝힌다.

- **ROLL** — 표에 `R = 2.5 t` 라고 적혀 있다. 46행 전부 맞는다.
- **PRESS** — 표에 `R = 3.5 t` 라고 적혀 있다. 49행 전부 맞는다.
- **SPSR** — 표에 아무 말이 없다.

SPSR 을 2.5t 로 넣었더니 두꺼운 행이 전부 가볍게 나왔다. 그래서 각 행의 **인쇄된
무게를 만들어내는 r 을 역산**했더니 이렇게 나왔다.

```
r/t   t ≤ 3.2  평균 1.96
      t > 3.2  평균 1.99      57행 중 55행이 1.88 ~ 2.11
```

**SPSR 은 R = 2.0 t.** 표가 말해주지 않은 값을 표 자신의 숫자로 되찾은 것이다.

## 원표의 오류 세 곳

**1. 마지막 블록의 이름이 틀렸다.** SPSR 표의 맨 아래 블록이 `250×250` 으로
한 번 더 적혀 있는데, 그 무게는 250 의 것이 아니다.

```
t 4.5 → 41.3      250×250 이면 34.0,   300×300 이면 41.1
t 6.0 → 54.7      250×250 이면 45.4,   300×300 이면 54.5
t 9.0 → 80.6      250×250 이면 66.4,   300×300 이면 80.2
t 12  → 106.0     250×250 이면 86.0,   300×300 이면 105.6
```

네 행이 전부 300×300 이다. `300x300` 으로 넣었다.

**2·3. 무게 두 개가 자기 표의 단면적과 어긋난다.** 계산 면적은 인쇄된 단면적과
0.01~0.04% 로 맞는데, 인쇄된 무게만 맞지 않는다.

| 호칭 | 인쇄 단면적 | 계산 면적 | 단면적 × 7.85 | 인쇄 무게 |
|---|---|---|---|---|
| 20×20×1.2 | 0.865 | 0.865 | **0.679** | 0.697 |
| 125×125×9 | 39.67 | 39.674 | **31.14** | 31.7 |

0.679 → 0.697 은 자리바꿈으로 보인다. 두 행 모두 `단위무게` 는 계산값을,
`표기중량` 은 인쇄값을 그대로 담고 있으니 판단은 쓰는 사람 몫이다.

## PLATE3D 에서

`SECT R` 은 `h b t r` 네 값만 받고 면적과 중량을 스스로 계산한다. 그래서 이
표에서 실제로 필요한 것은 **A, B, t, r** 뿐이고, `단면적`·`단위무게` 는 참고값이다.

사용자가 직접 치는 각관은 r 기본값을 **2.5t** 로 둔다.
